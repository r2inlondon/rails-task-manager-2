<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>Title: <%= link_to task.title, task_path(task) %> - Is the task Completed?  <%= task.completed %> </li>
  <% end %>
</ul>

<%= link_to "Create New Task", new_path  %>